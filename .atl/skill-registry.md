# Skill Registry

## Project
- Name: padex
- Root: `C:\Users\prueba\Desktop\Proyectos\padex`
- Generated by: `sdd-init`

## User Skills
| Skill | Source | Trigger / Use Case |
|---|---|---|
| branch-pr | user (`~/.config/opencode/skills`) | Creating pull requests or preparing review branches |
| go-testing | user (`~/.config/opencode/skills`) | Go tests, Bubbletea TUI testing, adding Go test coverage |
| issue-creation | user (`~/.config/opencode/skills`) | Creating GitHub issues for bugs or features |
| judgment-day | user (`~/.config/opencode/skills`) | Dual/adversarial reviews (`judgment day`, `juzgar`, `doble review`) |
| skill-creator | user (`~/.config/opencode/skills`) | Creating new AI skills or agent instructions |

## SDD Skills
| Skill | Purpose |
|---|---|
| sdd-init | Initialize project SDD context |
| sdd-explore | Investigate ideas before proposing changes |
| sdd-propose | Create change proposals |
| sdd-spec | Write delta specifications |
| sdd-design | Produce technical design |
| sdd-tasks | Break work into implementation tasks |
| sdd-apply | Implement approved tasks |
| sdd-verify | Validate implementation against specs |
| sdd-archive | Archive completed changes |

## Project Convention Files
- No `AGENTS.md`, `agents.md`, `CLAUDE.md`, `.cursorrules`, `GEMINI.md`, or `copilot-instructions.md` found in project root.
- Informational project doc detected: `INSTRUCCIONES.md` (run/setup notes and feature overview).

## Compact Rules

### JavaScript / Node (backend)
- Backend uses Node.js + Express in CommonJS modules.
- Data layer uses Sequelize models and PostgreSQL via `DATABASE_URL`.
- Prefer route -> service/model separation already present under `src/routes`, `src/services`, and `src/models`.
- Keep API contracts under `/api/*`; preserve request-id logging and centralized error handling in `src/index.js`.

### React Native / Expo (mobile)
- Mobile app uses Expo + React Native with plain JS (no TypeScript detected).
- Shared app state is provider-based (`AuthProvider`, `ThemeProvider`); prefer context/services over ad-hoc globals.
- Navigation lives under `src/navigation`; keep UI pieces in `src/components` and screen logic in `src/screens`.

### React / Vite (partners web)
- Partners web uses React + Vite with plain JSX and ESLint.
- Feature-oriented structure exists under `src/features`; prefer colocating feature UI/logic there.
- Respect existing ESLint flat config in `partners-web/eslint.config.js`.

### Testing / Quality
- No formal test runner detected anywhere in the monorepo; Strict TDD is unavailable until one is installed.
- Existing validation is mostly manual/ad-hoc scripts (`backend/test_*.js`) rather than framework-managed tests.
- Only confirmed quality tool is ESLint for `partners-web`; no formatter or type checker config detected.
