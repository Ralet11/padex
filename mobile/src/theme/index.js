// Re-export specific modules so they can be imported directly from 'theme'
export { defaultColors as colors } from './colors';
export { spacing, radius, shadows } from './spacing';
export { typography } from './typography';
